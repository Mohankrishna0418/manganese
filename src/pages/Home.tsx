import React from "react";
import NavigationBar from "../components/NavigationBar";

const Home = (): React.ReactNode => {
  return (
    <div className="home">
      <NavigationBar />
    </div>
  );
};

export default Home;
